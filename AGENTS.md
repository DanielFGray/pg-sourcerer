* this is a bun project. never use `npm` or `npx` use `bun` or `bunx`
* `ast-grep` is installed. it will be your most valuable tool during edits. you can execute `curl https://ast-grep.github.io/llms.txt` if you need further help to understand it
