{
  "name": "pgsourcerer-demo",
  "license": "Unlicense",
  "private": true,
  "type": "module",
  "scripts": {
    "dry": "bun -b --env-file=.env pgsourcerer generate --log-level debug --dry-run",
    "dev": "bun run-p db:watch generate:watch",
    "start": "bun run generate",
    "generate": "bun -b --env-file=.env pgsourcerer generate --log-level debug",
    "generate:watch": "bun --env-file=.env --watch pgsourcerer generate --log-level debug",
    "predev": "bun db:ensure",
    "gm": "bun -b graphile-migrate",
    "db:ensure": "bun scripts/dbEnsure.ts",
    "db:setup": "bun scripts/dbSetup.ts",
    "db:latest": "bun run gm watch --once",
    "db:watch": "bun run gm watch",
    "db:reset": "bun run gm reset --erase",
    "db:start": "docker compose up -d db",
    "db:stop": "docker compose stop db",
    "db:exec": "docker compose exec db",
    "kysely:codegen": "kysely-codegen --env-file=.env --dialect=postgres --log-level debug --exclude-pattern 'graphile*.*' --out-file=generated/DB.d.ts",
    "db:init": "run-s db:start db:setup db:reset db:latest",
    "env:init": "bun scripts/generateEnvFile.ts",
    "init": "NOCONFIRM=1 run-s -s env:init db:init",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@danielfgray/pg-sourcerer": "workspace:*",
    "@effect/cli": "catalog:",
    "@effect/platform": "catalog:",
    "@effect/platform-bun": "catalog:",
    "@effect/platform-node": "catalog:",
    "@effect/sql": "^0.49.0",
    "@effect/sql-pg": "^0.50.1",
    "@hono/standard-validator": "^0.2.1",
    "@orpc/server": "^1.13.2",
    "@trpc/server": "^11.8.1",
    "@types/express": "^5.0.6",
    "arktype": "^2.1.29",
    "effect": "^3.19.14",
    "elysia": "^1.4.21",
    "express": "^5.2.1",
    "graphile-migrate": "next",
    "hono": "^4.11.3",
    "inquirer": "^9.2.12",
    "npm-run-all": "^4.1.5",
    "valibot": "^1.2.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "kysely": "^0.28.9",
    "kysely-codegen": "^0.19.0",
    "prettier": "^3.2.4",
    "typescript": "catalog:"
  }
}
